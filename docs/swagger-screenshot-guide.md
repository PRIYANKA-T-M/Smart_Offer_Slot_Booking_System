# Swagger Screenshot Guide

For the submission, run the backend and navigate to the Swagger UI.

Take a screenshot showing:
1. `GET /api/Offers`
2. `POST /api/Offers`
3. `POST /api/Slots`
4. `POST /api/Bookings`
5. `PUT /api/Bookings/{id}/status`
